server:
  port: 0
logging:
  level:
    com.quorum.gauge: DEBUG
    org.apache.sshd.common.forward.DefaultForwardingFilter: DEBUG # to display port forwarding info

quorum:
  wallets:
    Wallet1:
      wallet-path: src/main/resources/wallets/key1
      wallet-pass:
    Wallet2:
      wallet-path: src/main/resources/wallets/key2
      wallet-pass:
    Wallet8:
      wallet-path: src/main/resources/wallets/key8
      wallet-pass:
